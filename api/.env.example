# Example environment variables for RadioChWeb API
# Copy to api/.env or to the target machine and edit as needed.
PORT=5001
APP_ENV=production
# Example SQLite path when running from the network share
DATABASE_URL=sqlite:///instance/radio_sources.db
# Add other env vars used by your app here
